export default {
  e2e: {
    baseUrl: 'https://dev.exploringnotboring.com',
    setupNodeEvents(on, config) {
      // aquí puedes agregar otros eventos válidos si los necesitas
      return config;
    },
  },
};

